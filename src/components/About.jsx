import React from 'react';
import { motion } from 'framer-motion';
import { styles } from '../styles';
import { services } from '../constants';
import { fadeIn, textVariant } from '../utils/motion';
import { SectionWrapper } from '../hoc';

const ServiceCard = ({ index, title, icon }) => {
  return (
    <motion.div
      variants={fadeIn('right', 'spring', 0.5 * index, 0.75)}
      className="xs:w-[250px] w-full card-gradient p-[1px] rounded-[20px] shadow-card">
      <div
        options={{
          max: 45,
          scale: 1,
          speed: 450,
        }}
        className="bg-jetLight rounded-[20px] py-5 px-12 min-h-[280px] flex justify-evenly items-center flex-col">
        <img src={icon} alt={title} className="w-16 h-16 object-contain" />
        <h3 className="text-taupe text-[18px] font-bold text-center">
          {title}
        </h3>
      </div>
    </motion.div>
  );
};

const About = () => {
  return (
    <div className="-mt-[6rem]">
      <motion.div variants={textVariant()}>
        <p className={styles.sectionSubText}>Introduction</p>
        <h2 className={styles.sectionHeadText}>Overview</h2>
      </motion.div>

      <motion.p
        variants={fadeIn('', '', 0.1, 1)}
        className="mt-4 text-taupe text-[18px] max-w-3xl leading-[30px]">
    As a passionate and self-driven developer, I focus on delivering clean, efficient, and impactful solutions. I bring a concierge-level approach to everything I build—prioritizing quality, responsiveness, and a smooth user experience. With a strong foundation in JavaScript, React.js, Node.js, and Python, I’m currently expanding into the fields of AI, Cloud Computing, and Cybersecurity.

   <b> My experience includes working as an SDE intern at a tech startup, building real-world applications under tight timelines, and now—I’m sharpening my full-stack development skills while diving deep into modern tech stacks.</b>



I value clear communication, reliable delivery, and a mindset of continuous learning. Whether I'm recreating projects, contributing to GitHub, or crafting technical notes in Obsidian, my goal is to grow every day and create solutions that are not just functional, but meaningful.

      </motion.p>

      <div className="mt-20 flex flex-wrap gap-10">
        {services.map((service, index) => (
          <ServiceCard key={service.title} index={index} {...service} />
        ))}
      </div>
    </div>
  );
};

export default SectionWrapper(About, 'about');